apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: webapis.deployments.k8s-controller.feiermanfamily.com
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  labels:
  {{- include "k8s-controller.labels" . | nindent 4 }}
spec:
  group: deployments.k8s-controller.feiermanfamily.com
  names:
    kind: WebAPI
    listKind: WebAPIList
    plural: webapis
    singular: webapi
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WebAPI is the Schema for the webapis API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: WebAPISpec defines the desired state of WebAPI
            properties:
              foo:
                description: Foo is an example field of WebAPI. Edit webapi_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: WebAPIStatus defines the observed state of WebAPI
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []